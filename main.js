a = "a" //this is just to make github not drop an error (file is empty)

function letgo() { //this is just to make github not drop an error (file is empty)
    a = "b" //this is just to make github not drop an error (file is empty)
} //this is just to make github not drop an error (file is empty)